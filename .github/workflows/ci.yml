name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore CrmLstg.slnx

      - name: Build
        run: dotnet build CrmLstg.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test CrmLstg.slnx --configuration Release --no-build --verbosity normal
